# ── CARU CARS ───────────────────────────────────────────────────────────────────
RewriteEngine On

# Legacy URL redirects (DealerCenter / Carsforsale → new pages)
RewriteRule ^loanapplication\.aspx$ /apply.html [R=301,L,NC]
RewriteRule ^loan-application$     /apply.html [R=301,L,NC]
RewriteRule ^finance$              /apply.html [R=301,L,NC]
RewriteRule ^apply$                /apply.html [R=301,L,NC]
RewriteRule ^inventory\.aspx$      /inventory.html [R=301,L,NC]
RewriteRule ^contact\.aspx$        /contact.html [R=301,L,NC]

# Block direct web access to log + CSV files
# TEMPORARY: allow DealerCenter CSV download with secret UA token so the
# GitHub Actions sync can fetch the feed. Revert once OPcache is sorted.
<FilesMatch "^(sync-log\.txt|dealercenter_feed\.csv)$">
    Require all denied
</FilesMatch>
<FilesMatch "^DealerCenter_.*\.csv$">
    SetEnvIf User-Agent "^carucars-pull/gK7pQn2vX9wR$" csv_allow
    Require env csv_allow
</FilesMatch>

# inventory-data.js must stay fresh — customers see sold cars if it's cached.
<Files "inventory-data.js">
    <IfModule mod_headers.c>
        Header set Cache-Control "public, max-age=300, must-revalidate"
        Header unset Expires
    </IfModule>
</Files>
