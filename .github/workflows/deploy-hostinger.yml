name: Deploy to Hostinger

on:
  push:
    branches: [main]
    paths-ignore:
      - 'inventory-data.js'
      - 'README.md'
      - '.github/workflows/sync-inventory.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get update -qq && sudo apt-get install -y -qq lftp

      - name: Deploy PHP + assets via FTP
        env:
          FTP_USER: ${{ secrets.FTP_USERNAME }}
          FTP_PASS: ${{ secrets.FTP_PASSWORD }}
        run: |
          lftp -c "
            set ftp:ssl-allow no;
            set ftp:passive-mode true;
            set net:timeout 30;
            set net:max-retries 3;
            set net:reconnect-interval-base 5;
            set mirror:use-pget-n 1;
            open -u \"$FTP_USER\",\"$FTP_PASS\" ftp://82.25.87.207;
            mirror --reverse --verbose --parallel=2 \
              --exclude-glob .git \
              --exclude-glob .git/ \
              --exclude-glob .github \
              --exclude-glob .github/ \
              --exclude-glob backups/ \
              --exclude-glob DealerCenter_*.csv \
              --exclude-glob dealercenter_feed.csv \
              --exclude build-inventory.py \
              --exclude-glob .claude/ \
              --exclude-glob .netlify/ \
              --exclude-glob .DS_Store \
              --exclude sync-log.txt \
              --exclude sync.lock \
              ./ /public_html/;
            bye
          "
